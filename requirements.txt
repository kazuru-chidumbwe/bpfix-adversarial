# Core offline workflow is stdlib-only (Python 3.10+).
# Optional extras (see pyproject.toml / docs/DEPENDENCIES.md):
#   pip install -e ".[lab]"     # paramiko, for SSH lab capture helpers
#   pip install -e ".[all]"
