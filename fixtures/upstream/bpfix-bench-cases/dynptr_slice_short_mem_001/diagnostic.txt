error[BPFIX-E005]: memory-object access exceeds the verifier-visible object size
  = class: source_bug
  = confidence: medium
  = diagnostic: supported, help: repair_hint, span: exact_pc
  = next action: bounds
  --> buggy.bpf.c:35
   |
29 | if (bpf_dynptr_from_xdp(ctx, 0, &ptr))
   | -------------------------------------- verifier still sees R0 as scalar(smin=0,smax=-281474976710656,umax=18446462598732840960), so the required scalar or map-value bound is not available at the use
35 | if (bpf_ntohs(eth->h_proto) != ETH_P_IP)
   | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ this memory access crosses the verifier-reported object size
   |
   = verifier[56]: invalid access to memory, mem_size=13 off=12 size=2; R0 min value is outside of the allowed memory range
   = note: nearest BPF instruction pc 15
   = note: parsed 14 verifier state snapshots
   = note[verifier-state]: verifier state shows the rejected memory pointer has a fixed object size and the access crosses that object boundary
   = required proof: prove the memory pointer offset plus access width stays inside the verifier-reported object size at the rejected access
help: Keep dynptr slice or helper-returned memory accesses inside the verifier-reported object size; clamp the offset and width before dereferencing the memory pointer.
