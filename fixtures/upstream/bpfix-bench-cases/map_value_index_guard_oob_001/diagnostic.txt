error[BPFIX-E005]: map-value access exceeds the verifier-visible value size
  = class: source_bug
  = confidence: medium
  = diagnostic: supported, help: repair_hint, span: exact_pc
  = next action: bounds
  --> buggy.bpf.c:48
   |
41 | if (selector != 0 && selector != 2 && selector != 5)
   | ---------------------------------------------------- nearby source context for scalar range
48 | return cfg->slots[selector] ? XDP_DROP : XDP_PASS;
   | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ this map-value access crosses the declared value size
   |
   = verifier[88]: invalid access to map value, value_size=12 off=20 size=4; R1 min value is outside of the allowed memory range
   = note: nearest BPF instruction pc 25
   = note: parsed 21 verifier state snapshots
   = note[verifier-state]: verifier state shows the rejected map-value pointer access crosses the declared value size
   = required proof: prove the map-value field offset plus access width stays below the declared map value size at the rejected access
help: Keep map-value field offsets, array indexes, and helper lengths inside the declared map value size; resize the map value or clamp the access before the load, store, or helper call.
