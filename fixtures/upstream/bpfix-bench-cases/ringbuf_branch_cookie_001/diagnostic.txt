error[BPFIX-E006]: verifier-visible compiler lowering hides the required proof
  = class: lowering_artifact
  = confidence: medium
  = diagnostic: supported, help: repair_hint, span: exact_pc
  = next action: provenance
  --> buggy.bpf.c:59
   |
35 | if ((void *)(eth + 1) > data_end)
   | --------------------------------- nearby source context for pointer provenance
47 | } else if (iph->protocol == IPPROTO_TCP) {
   | ------------------------------------------ verifier state changes from pkt to scalar before the rejected access
54 | if (!rec)
   | --------- nearby source context for pointer provenance
59 | asm volatile("%[cookie] <<= 32; %[cookie] >>= 32" : [cookie] "+r"(cookie));
   | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ rejected here: verifier sees a scalar where a pointer is required
   |
   = verifier[76]: R1 pointer arithmetic with <<= operator prohibited
   = note: nearest BPF instruction pc 25
   = note: parsed 19 verifier state snapshots
   = note[lowering]: a verifier-tracked pointer reaches a left-shift instruction, which the verifier rejects because the operation destroys pointer provenance
   = required proof: preserve a verifier-recognized pointer type at the operation that requires a pointer
help: Reacquire a verifier-tracked pointer before the rejected dereference.
help: Remove only the pointer-destroying asm shift; keep the verifier-tracked pointer value and use that checked pointer for the final memory access.
help: Preserve the source-level cookie/shadow alias by casting the reserved ringbuf record to cookie and back without arithmetic before submit.
help: Keep the final access on a verifier-tracked pointer; rederive it from a checked base after scalar work.
